<div id="missing" class="ooo">
Missing:
<%= "-" unless @team.all_missing_teammembers.length > 0%>
<% @team.all_missing_teammembers.each do |teammember| %>
<%= teammember.name %><%= "," unless teammember.eql?(@team.all_ooo_teammembers.last) %>
<% end %>
</div>
