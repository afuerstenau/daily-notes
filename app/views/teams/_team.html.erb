<div class="subteam">
  <strong><%= link_to team.name, team %></strong><br>
  <%= render (team.teammembers) unless team.nil? %>
</div>
<% if team.id ==1 %>
<div style="clear: both;"/>
<% end%>
<%= render (team.subteams) unless team.nil? %>
