<li>
<% if goal.link.blank? %>
<%= goal.summary %>
<% else %>
<%= link_to goal.summary, goal.link %>
<% end %>
<%= %><span id="goal_buttons"><%= link_to 'Edit', edit_goal_path(goal)%> <%= link_to 'Destroy', goal, method: :delete, data: { confirm: 'Are you sure?' } %></span>
</li>

        